name: app
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  provider: ^6.1.2
  shared_preferences: ^2.2.2
  google_fonts: ^6.2.1
  cupertino_icons: ^1.0.8
  flutter_animate: ^4.5.0
  percent_indicator: ^4.2.3
  modal_bottom_sheet: ^3.0.0
  shamsi_date: ^1.0.0
  
  # پکیج‌های حساس
  timezone: ^0.9.2
  flutter_local_notifications: ^18.0.1
  vibration: ^3.1.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true

# *** بخش حیاتی برای حل مشکل شما ***
# این بخش پروژه را مجبور می‌کند دقیقاً از همین نسخه استفاده کند
dependency_overrides:
  flutter_local_notifications: 18.0.1
